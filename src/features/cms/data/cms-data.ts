import {
  initialPosts,
  initialCategories,
  initialTags,
  type Post,
  type Category,
  type Tag,
} from "@/data/cms";

export type { Post, Category, Tag };
export { initialPosts, initialCategories, initialTags };
